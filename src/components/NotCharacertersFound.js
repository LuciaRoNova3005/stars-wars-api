const NotCharactersFound = (props) => {
  return (
    <section className="">
      <p className="text">There are not characters that match your search</p>
    </section>
  );
};

export default NotCharactersFound;
